services:
  # =================================
  # PostgreSQL Database
  # =================================
  postgres:
    image: postgres:15-alpine
    container_name: homebase-cortex-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: homebase_cortex
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - homebase-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # =================================
  # Next.js Application
  # =================================
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: homebase-cortex-app
    restart: unless-stopped
    ports:
      - "3001:3000"
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/homebase_cortex
      NEXT_PUBLIC_DB_ENV: docker
      NEXTAUTH_SECRET: homebase-cortex-secret-key
      NEXTAUTH_URL: http://localhost:3001
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - homebase-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # =================================
  # Development Mode (hot reload)
  # =================================
  app-dev:
    image: node:20-alpine
    container_name: homebase-cortex-dev
    working_dir: /app
    command: sh -c "npm install && npx prisma generate && npm run dev"
    ports:
      - "3001:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/homebase_cortex
      NEXT_PUBLIC_DB_ENV: docker
      NEXTAUTH_SECRET: development-secret-key-12345
      NEXTAUTH_URL: http://localhost:3001
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - homebase-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    profiles:
      - dev

networks:
  homebase-network:
    driver: bridge

volumes:
  postgres_data:
